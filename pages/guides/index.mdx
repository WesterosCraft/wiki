import { Tab, Tabs } from 'nextra-theme-docs';
import Card, { Cards } from '../../components/Card';
import gettingStartedGuides from './getting-started/_meta.json';
import howToGuides from './how-to/_meta.json';
import resourcesGuides from './resources/_meta.json';
import rulesAndGuidelinesGuides from './rules-and-guidelines/_meta.json';

# Guides

This section of the docs includes guides for downloading the modpack, as well as guides for building on WesterosCraft.

<Tabs items={['Getting Started', 'How To', 'Resources', 'Rules and Guidelines']}>
  <Tab style={{ paddingTop: 8 }}>
    <Cards
      num={3}
      children={Object.keys(gettingStartedGuides).map((key, i) => (
        <Card
          key={i}
          title={gettingStartedGuides[key].title}
          icon={gettingStartedGuides[key].icon}
          href={`/guides/getting-started/${key}` ?? ''}
        />
      ))}
    />
  </Tab>
  <Tab>
    <Cards
      num={3}
      children={Object.keys(howToGuides).map((key, i) => (
        <Card
          key={i}
          title={howToGuides[key].title}
          icon={howToGuides[key].icon}
          href={`/guides/how-to/${key}` ?? ''}
        />
      ))}
    />
  </Tab>
  <Tab>
    <Cards
      num={3}
      children={Object.keys(resourcesGuides).map((key, i) => (
        <Card
          key={i}
          title={resourcesGuides[key].title}
          icon={resourcesGuides[key].icon}
          href={`/guides/resources/${key}` ?? ''}
        />
      ))}
    />
  </Tab>
  <Tab>
    <Cards
      num={3}
      children={Object.keys(rulesAndGuidelinesGuides).map((key, i) => (
        <Card
          key={i}
          title={rulesAndGuidelinesGuides[key].title}
          icon={rulesAndGuidelinesGuides[key].icon}
          href={`/guides/rules-and-guidelines/${key}` ?? ''}
        />
      ))}
    />
  </Tab>
</Tabs>
