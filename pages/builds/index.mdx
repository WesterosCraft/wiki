import { Tab, Tabs } from 'nextra-theme-docs';
import Card, { Cards } from '../../components/Card';
import beyondTheWall from './beyond-the-wall/_meta.json';
import crownlands from './crownlands/_meta.json';
import dorne from './dorne/_meta.json';
import ironIslands from './iron-islands/_meta.json';
import north from './north/_meta.json';
import reach from './reach/_meta.json';
import riverlands from './riverlands/_meta.json';
import stormlands from './stormlands/_meta.json';
import theWall from './the-wall/_meta.json';
import vale from './vale/_meta.json';
import westerlands from './westerlands/_meta.json';

# Builds

All WesterosCraft Builds

<Tabs
  items={[
    'Beyond The Wall',
    'Crownlands',
    'Dorne',
    'Iron Islands',
    'North',
    'Reach',
    'Riverlands',
    'Stormlands',
    'The Wall',
    'Vale',
    'Westerlands',
  ]}
>
  <Tab style={{ paddingTop: 8 }}>
    <Cards
      num={3}
      children={Object.keys(beyondTheWall).map((key, i) => (
        <Card
          key={i}
          title={beyondTheWall[key].title}
          description={beyondTheWall[key].description}
          icon={beyondTheWall[key].icon}
          href={beyondTheWall[key].href ?? ''}
        />
      ))}
    />
  </Tab>
  <Tab>
    <Cards
      num={3}
      children={Object.keys(crownlands).map((key, i) => (
        <Card
          key={i}
          title={crownlands[key].title}
          description={crownlands[key].description}
          icon={crownlands[key].icon}
          href={crownlands[key].href ?? ''}
        />
      ))}
    />
  </Tab>
  <Tab>
    <Cards
      num={3}
      children={Object.keys(dorne).map((key, i) => (
        <Card
          key={i}
          title={dorne[key].title}
          description={dorne[key].description}
          icon={dorne[key].icon}
          href={dorne[key].href ?? ''}
        />
      ))}
    />
  </Tab>
  <Tab>
    <Cards
      num={3}
      children={Object.keys(ironIslands).map((key, i) => (
        <Card
          key={i}
          title={ironIslands[key].title}
          description={ironIslands[key].description}
          icon={ironIslands[key].icon}
          href={ironIslands[key].href ?? ''}
        />
      ))}
    />
  </Tab>
  <Tab>
    <Cards
      num={3}
      children={Object.keys(north).map((key, i) => (
        <Card
          key={i}
          title={north[key].title}
          description={north[key].description}
          icon={north[key].icon}
          href={north[key].href ?? ''}
        />
      ))}
    />
  </Tab>
  <Tab>
    <Cards
      num={3}
      children={Object.keys(reach).map((key, i) => (
        <Card
          key={i}
          title={reach[key].title}
          description={reach[key].description}
          icon={reach[key].icon}
          href={reach[key].href ?? ''}
        />
      ))}
    />
  </Tab>
  <Tab>
    <Cards
      num={3}
      children={Object.keys(riverlands).map((key, i) => (
        <Card
          key={i}
          title={riverlands[key].title}
          description={riverlands[key].description}
          icon={riverlands[key].icon}
          href={riverlands[key].href ?? ''}
        />
      ))}
    />
  </Tab>
  <Tab>
    <Cards
      num={3}
      children={Object.keys(stormlands).map((key, i) => (
        <Card
          key={i}
          title={stormlands[key].title}
          description={stormlands[key].description}
          icon={stormlands[key].icon}
          href={stormlands[key].href ?? ''}
        />
      ))}
    />
  </Tab>
  <Tab>
    <Cards
      num={3}
      children={Object.keys(theWall).map((key, i) => (
        <Card
          key={i}
          title={theWall[key].title}
          description={theWall[key].description}
          icon={theWall[key].icon}
          href={theWall[key].href ?? ''}
        />
      ))}
    />
  </Tab>
  <Tab>
    <Cards
      num={3}
      children={Object.keys(vale).map((key, i) => (
        <Card
          key={i}
          title={vale[key].title}
          description={vale[key].description}
          icon={vale[key].icon}
          href={vale[key].href ?? ''}
        />
      ))}
    />
  </Tab>
  <Tab>
    <Cards
      num={3}
      children={Object.keys(westerlands).map((key, i) => (
        <Card
          key={i}
          title={westerlands[key].title}
          description={westerlands[key].description}
          icon={westerlands[key].icon}
          href={westerlands[key].href ?? ''}
        />
      ))}
    />
  </Tab>
</Tabs>
